# Marketing copy for font-copier

## Launch tweet

Shipped `font-copier`: show Claude a font you like, walk away with your own OTF of it. Renders 82 glyphs at 2000px/em, traces with potrace, rebuilds a TrueType `glyf` table — no hand-coded beziers, no Glyphs license.

github.com/codyhxyz/font-copier

## Alt tweets

- New Claude plugin: `font-copier`. Drop a specimen image → get a renamed OTF matching its silhouette. OFL base from Google Fonts, traced glyph-by-glyph, contour directions flipped the way TrueType wants. github.com/codyhxyz/font-copier
- `font-copier` is a Claude plugin for people who keep saying "I wish I could just use this font." 82 glyphs traced at 2000px/em, rebuilt into an OTF, renamed, specimen page opens in your browser. github.com/codyhxyz/font-copier
- Most "LLM made a font" demos hand-code bezier curves and look amateur. `font-copier` doesn't: render → threshold → potrace → `Cu2QuPen(reverse_direction=True)` → clean OTF. github.com/codyhxyz/font-copier
